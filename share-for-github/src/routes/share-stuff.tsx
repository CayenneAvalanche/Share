                  {b.offer != null && b.offer > 0 && (
                    <Badge variant="secondary">
                      up to {formatCurrency(b.offer)}
                    </Badge>
                  )}
